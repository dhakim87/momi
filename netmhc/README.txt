This is where netMHCIIpan output results, checkpoint files, and epitope scoring is stored.  
The pipeline allows restarting and tracks its progress using files in this directory, so clearing it starts over

